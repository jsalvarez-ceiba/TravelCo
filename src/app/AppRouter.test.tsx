import React from 'react';
import {render} from '@testing-library/react';
import AppRouter from './AppRouter';

describe('AppRoute Test', () => {
    test('Snapshot', () => {
        expect(true).toBeTruthy();
    });
});