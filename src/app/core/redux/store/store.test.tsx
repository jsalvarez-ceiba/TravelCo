import {render} from '@testing-library/react';
import store from './store';

describe('store testing', () => {
    
    test('should ', () => {

        const storeTest = render(store);

        expect(storeTest).toBeTruthy();




        
    });
    
    
    
});
